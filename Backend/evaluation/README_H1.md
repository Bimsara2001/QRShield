# H1 evidence collection

Run these scripts from `Backend/` after the persistent isolated network and
`qrshield-egress-proxy` are running. They use only the harmless URLs embedded
in the scripts and do not change the QRShield analysis pipeline.

Generated evidence excludes page HTML, screenshot bytes, credentials, API
keys, Docker IDs, and machine-specific identifiers.
